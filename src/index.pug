html
  head
    meta(charset="utf-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    script(src="https://preview.babylonjs.com/babylon.js")
    script(src="https://unpkg.com/animejs/lib/anime.min.js")
    if project.ENV === "production"
      script(src=`/${pkg.name}/js/script.js`)
      link(rel="stylesheet" href=`/${pkg.name}/css/style.css`)
    else
      script(src="/js/script.js")
      link(rel="stylesheet" href="/css/style.css")
    title Aritig-L - Academic Council - Digital Samadhi

  body
    div#babylon-wrapper
      canvas#babylon-engine
      r-grid#content-wrapper(columns="12")
        r-cell#title(span="12")
          h1#title-text
        r-cell(span="2-6")
          h2 Welcome to...
          h2 You will entere an experience...
          h2 Press #[a(href="#") >] to #[a(href="#") begin] your digital expereience...
        r-cell(span="5")
          dl
            dt ^samadhi
            dd a state of intense concentration achieved through meditation. In Hindu yoga this is regarded as the final stage, at which union with the divine is reached (before or at death).

    script main('#{pkg.name}', '#{project.ENV}')
